name: Esteira CI

on:
  push:
    branches: [ "feature/*" ]

jobs:
  build:
    permissions: write-all
    runs-on: ubuntu-latest
    strategy:
      matrix:
        java-version: [ 11 ]
    steps:
      - name: Check git
        uses: actions/checkout@v2

      - name: Setup Java JDK
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Build Maven
        run: mvn clean install

      # - name: Run unit tests
      #  run: mvn test

      - name: SonarCloud Scan
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: |
          wget https://binaries.sonarsource.com/Distribution/sonar-scanner-cli/sonar-scanner-cli-4.6.2.2472-linux.zip
          unzip sonar-scanner-cli-4.6.2.2472-linux.zip
          export PATH=$PATH:$(pwd)/sonar-scanner-4.6.2.2472-linux/bin
          sonar-scanner -Dsonar.projectKey=Filmaro_trabalho-edsII -Dsonar.organization=filmaro -Dsonar.sources=src -Dsonar.host.url=https://sonarcloud.io

      - name: SonarCloud Scan
        uses: sonarsource/sonarcloud-github-action@master
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}

      #- name: Create a gitHub release/tag
      #  id: release
      #  uses: release-drafter/release-drafter@v5
      #  env:
      #    GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      #- name: Install maven
      #  uses: actions/setup-java@v1
      #  with:
      #    java-version: 11

      #- name: Update maven version
      #  run: mvn -B versions:set -DnewVersion=${{ steps.release.outputs.tag_name }} -DgenerateBackupPoms=false

      #- name: Auto commit
      #  uses: stefanzweifel/git-auto-commit-action@v4
